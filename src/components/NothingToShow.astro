---
const { filter=[], } = Astro.props;
---

{filter.length==0 && <div class='flex flex-col sm:justify-center items-center h-full w-full'>
          <img src='/placehoder.jpg'/>
          <p class='text-gray-500 text-3xl'>No Forms to display</p>
          </div>}